{
  "name": "replay-session-ui-filter",
  "type": "project",
  "tags": "replay harness, session ui filter, task detail, ui routes",
  "body": "Replay harness fires real prompts with `session_id` prefixed `\"replay-\"`. These log T1 events to `task_events` against the active task, appearing as noise in the /ui/tasks/ detail panel.\n\nFix (`hooks/ui/routes.py::ui_task_detail`, moved out of `hooks/server.py` when the UI code was split into `hooks/ui/routes.py` + `hooks/ui/deps.py`): filter at display time:\n```python\nturns = [e for e in history if e.get(\"tools\") != \"decision\" and not e.get(\"session_id\", \"\").startswith(\"replay-\")]\n```\n\n**Why:** Fix at display time not write time — replay events in task_events remain useful for test diagnostics; only the UI view is filtered.\n\n**Historical cleanup:** 1025 replay rows were deleted from proj_tasks.db task_events in June 2026 — future replay rows remain in DB but are hidden in UI.\n\n**Correction (2026-07-05):** file location updated — logic itself was unchanged, only the module it lives in.",
  "files": [],
  "docs": [],
  "related": [
    "claude-hooks-replay-harness",
    "tasks-ui-live-session-gate"
  ],
  "updated": "2026-07-14 14:26:23"
}
